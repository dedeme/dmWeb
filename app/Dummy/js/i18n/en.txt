# File generated by jsi18n.

# src/core/Backups.js: 55
'%0' can not be read = '%0' can not be read

# src/core/Backups.js: 190
'%0' is an empty file = '%0' is an empty file

# src/core/Chpass.js: 72
# src/core/Auth.js: 50
Accept = Accept

# src/core/Backups.js: 192
# src/core/Backups.js: 228
All the data will be replaced = ATENTION!: All the data will be replaced.\nContinue?

# src/core/Dom.js: 75
Backs = Backups

# src/core/Backups.js: 181
Backup file is missing = Backup file is missin

# src/core/Backups.js: 237
Backups = Backups

# src/core/Chpass.js: 76
Cancel = Cancel

# src/core/Settings.js: 66
Change Language to %0 = Change Language to %0

# src/core/Settings.js: 73
Change Password = Change Password

# src/core/Chpass.js: 192
# src/core/Auth.js: 177
Check gray squares = Check gray squares

# src/core/Backups.js: 221
Clear = Clear

# src/core/Backups.js: 218
Clear trash? = Clear trash?

# src/core/Expired.js: 44
Click %0 to continue. = Click %0 to continue.

# src/core/Chpass.js: 148
Confirm password = Confirm password

# src/core/Chpass.js: 106
Confirm password is missing = 'Confirm password' is missing

# src/Create.js: 29
# src/Create.js: 30
# src/core/Dom.js: 71
Create = Create

# src/core/Chpass.js: 136
Current password = Current password

# src/core/Chpass.js: 94
Current password is missing = 'Current password' is missing

# src/core/Backups.js: 240
Download = Download

# src/core/Chpass.js: 177
Fail trying to change password = Fail trying to change password

# src/core/Backups.js: 88
Fail unzipping backup = Fail unzipping backup

# src/core/Backups.js: 90
File is not a Selectividad backup = File is not a Selectividad backup

# src/core/Chpass.js: 90
# src/core/Auth.js: 104
Grey squares checks are wrong = Grey squares checks are wrong

# src/core/Dom.js: 42
Help & Credits = Help & Credits

# src/core/Auth.js: 139
Keep connected = Keep connected

# src/core/Chpass.js: 212
# src/core/Auth.js: 197
Login = Login

# src/core/Bye.js: 40
Logout-message = <p>%0 has finished.</p><p><b>Good by!</b></p>

# src/core/Backups.js: 161
Make backup = Make backup

# src/core/Chpass.js: 142
New password = New password

# src/core/Chpass.js: 112
New password and confirm password do not match = New password and confirm password do not match

# src/core/Chpass.js: 100
New password is missing = 'New password' is missing

# src/core/Backups.js: 185
Only one file can be selected = Only one file can be selected

# src/core/Auth.js: 124
Password = Password

# src/core/Auth.js: 81
Password is missing = Password is missing

# src/core/Chpass.js: 45
Password successfully changed = Password successfully changed

# src/core/Backups.js: 245
Restore = Restore

# src/core/Backups.js: 178
Restore backup = Restore backup

# src/core/Expired.js: 42
Session is expired. = Session is expired.

# src/core/Dom.js: 77
# src/core/Settings.js: 82
Settings = Settings

# src/core/Backups.js: 216
Trash = Trash

# src/Update.js: 29
# src/Update.js: 30
# src/core/Dom.js: 69
Update = Update

# src/core/Auth.js: 119
User = User

# src/core/Auth.js: 77
User name is missing = User name is missing

# src/core/Auth.js: 162
Wrong password = Wrong password

# src/core/Expired.js: 26
here = here

